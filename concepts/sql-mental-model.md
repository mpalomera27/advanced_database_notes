# SQL Mental Model

## My understanding
SQL works on tables where each row is a real instance and each column is a shared attribute, and queries basically tell the language what info I want, not how to get it.

## Why it matters
When queries are pretty long, having the correct mental model makes everything less confusing and helps to debug wrong results.

## Example
A table named "movies" represents the entity "Movie", while each row is a specific film.