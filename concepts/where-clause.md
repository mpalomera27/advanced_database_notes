# WHERE clause

## My understanding
The WHERE clause filters rows by applying conditions before the info we want is shown.

## Why it matters
Queries return too much data when WHERE is not used, so they become difficult to interpret.

## Example
SELECT * FROM movies WHERE year > 2010;